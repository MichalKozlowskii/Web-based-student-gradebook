spring:
  datasource:
    url: jdbc:mysql://localhost:3306/authdb?serverTimezone=Europe/Warsaw
    username: root
    password: 123456
  data:
    redis:
      password: 123456

usos:
  consumerKey:
  consumerSecret:

auth:
  keys:
    private-key-path:
    public-key-path:
  encryption:
    key: